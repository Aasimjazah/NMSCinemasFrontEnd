import { ClickcolorDirective } from './clickcolor.directive';

describe('ClickcolorDirective', () => {
  it('should create an instance', () => {
    const directive = new ClickcolorDirective();
    expect(directive).toBeTruthy();
  });
});
